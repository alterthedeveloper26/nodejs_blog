<div class="mt-4 ml-4">
  <div class="row">
    <div class="col-lg-3">
      <button>Learn now</button>
    </div>
    <div class="col-lg-9">
      <h2>{{course.name}}</h2>
      <img src="{{course.image}}" alt="" />
      <p>{{course.description}}</p>
    </div>
  </div>
</div>